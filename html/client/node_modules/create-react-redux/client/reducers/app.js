const initialState = {}
import {INIT} from '../actions/types'

const contacts = (state = initialState, action) => {
  switch (action.type) {
    case INIT :
      return state;
    default:
      return state;
  }
};

export default contacts;
